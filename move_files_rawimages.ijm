
///////  SCRIPT TO MOVE ALL IMAGES IN A FOLDER TO NEW SUBFOLDER NAMED RAWIMAGES       											                   				   	
///////  WRITTEN BY: SARADHA VENKATACHALAPATHY                                                                                                                     
///////  ASSUMPTIONS: The folder contains only images. Tested on ImageJ v1.52p.
///////  DESCRIPTION: The script accepts the directory to a folder containing images. It creates and moves all the images to a sub-folder named "rawimages".


dirsa = getArgument();	// accepts the directory to the folder
setBatchMode(true);		// run in batch mode to save time and memory

filenamesa=getFileList(dirsa);	// get the list of names of the images in the folder

dirraw= dirsa + "rawimages"+ File.separator;  // define the path to a new folder
File.makeDirectory(dirraw); 				  // create the path

// move all images to the new subfolder
for(f=0;f<filenamesa.length;f++){
	path1=dirsa+filenamesa[f];
	path2=dirraw+filenamesa[f];
	File.rename(path1, path2);
}

